import { ConnectType, WalletInfo, WalletStatus } from '@terra-dev/use-wallet';
import { NetworkInfo } from '@terra-dev/wallet-types';
import { ReactNode } from 'react';
export interface StaticWalletProviderProps {
    children: ReactNode;
    defaultNetwork: NetworkInfo;
    status?: WalletStatus;
    availableConnectTypes?: ConnectType[];
    availableInstallTypes?: ConnectType[];
    wallets?: WalletInfo[];
}
export declare function StaticWalletProvider({ children, defaultNetwork, status, availableConnectTypes, availableInstallTypes, wallets, }: StaticWalletProviderProps): JSX.Element;
